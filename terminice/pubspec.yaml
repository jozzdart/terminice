name: terminice
description: "A library for creating and managing terminal interfaces."
resolution: workspace
version: 0.0.1
homepage:

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:

dev_dependencies:
  lints:
